# PocketFM Argus (Open WebUI) (OpenAI-compatible) — copy to .env and fill in.
# .env is gitignored; never commit a real key.
ARGUS_API_KEY=sk-your-key-here
ARGUS_BASE_URL=https://argus.pocketfm.org/api
ARGUS_MODEL=gpt-4o
